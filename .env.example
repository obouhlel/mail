# Timezone configuration
TZ=Europe/Paris

# Hostname for the mail server
HOSTNAME=mail.example.com

# Disable optional services to save resources (optional)
DISABLE_CLAMAV=TRUE
DISABLE_RSPAMD=TRUE
DISABLE_ROUNDCUBE=TRUE

# HTTPS configuration (optional)
HTTPS=OFF

# Data directory for persistent storage
DATA_DIR=/data